OE口低电平使能（使用74HV595时将OE口和GND短接）；RCLK寄存器时钟；SRCLR(一拔)串行清零口（低电平使能）；SRCLK(串行时钟)：输出时钟，SER（串行数据）:输入数据；

SER(P34)输入一位数据（0/1），SRCLK（P36）时钟上升沿到来时，SER中的数据移位进入移位寄存器，在8个上升沿过去后，一共8位数据在寄存器时钟RCLK的上升沿到来时并行输出到QA~QH，实现I/O口扩展。

多片级联：将QH'接到另一片74HC595的SER口，所有74HC595的RCLK接同一个时钟，实现级联。多片74HC595级联可实现16位、24位、32位I/O口扩展

